#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isalpha - don't know
 * @c: integer
 * Return: alphabets
 */

int _isalpha(int c)
{
	return (isalpha(c));
}
