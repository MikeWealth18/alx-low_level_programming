#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strcpy - function
 * @dest: don't
 * @src: character
 * Return: copy of string
 */

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
